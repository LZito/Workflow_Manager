# Disable Gradle's native POSIX layer — required on WSL/NTFS where chmod always fails.
systemProp.org.gradle.native=false

# Application version — updated automatically by: gradle release -PprojectVersion=x.y.z
projectVersion=1.0.5
